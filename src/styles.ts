import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  scroll: {
    height: '100%',
    alignItems: 'center',
    gap: 40,
  },
  landscape: {
    marginTop: 20,
    gap: 20,
  },
});

export default styles;
